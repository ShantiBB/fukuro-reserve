version: v2
lint:
  use:
    - STANDARD
    - SERVICE_SUFFIX
  except:
    - RPC_REQUEST_STANDARD_NAME
breaking:
  use:
    - FILE
    - WIRE_JSON
    - PACKAGE

modules:
  - path: proto

deps:
  - buf.build/bufbuild/protovalidate